from scripts.common_imports import *
def main():
    SPATIAL_GRID_SIZE = 1000  # meters (square binnings of SPATIAL_GRID_SIZE * SPATIAL_GRID_SIZE)
    TIME_BIN_MINUTES = 30 
    MAX_SPEED_KMH = 180 

if __name__ == "main":
    main()